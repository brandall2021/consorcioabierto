// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: consorcios.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createConsorcio = `-- name: CreateConsorcio :one
INSERT INTO consorcios (tenant_id, nombre, cuit, domicilio, tipo)
VALUES (app.current_tenant_id(), $1::TEXT, $2::TEXT,
        $3::TEXT, $4::TEXT)
RETURNING tenant_id, id, nombre, cuit, domicilio, tipo, estado, config, created_at, updated_at
`

type CreateConsorcioParams struct {
	Nombre    string      `json:"nombre"`
	Cuit      pgtype.Text `json:"cuit"`
	Domicilio pgtype.Text `json:"domicilio"`
	Tipo      string      `json:"tipo"`
}

func (q *Queries) CreateConsorcio(ctx context.Context, arg CreateConsorcioParams) (Consorcio, error) {
	row := q.db.QueryRow(ctx, createConsorcio,
		arg.Nombre,
		arg.Cuit,
		arg.Domicilio,
		arg.Tipo,
	)
	var i Consorcio
	err := row.Scan(
		&i.TenantID,
		&i.ID,
		&i.Nombre,
		&i.Cuit,
		&i.Domicilio,
		&i.Tipo,
		&i.Estado,
		&i.Config,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getConsorcio = `-- name: GetConsorcio :one
SELECT tenant_id, id, nombre, cuit, domicilio, tipo, estado, config, created_at, updated_at
FROM consorcios
WHERE tenant_id = app.current_tenant_id()
  AND id = $1::UUID
`

func (q *Queries) GetConsorcio(ctx context.Context, id pgtype.UUID) (Consorcio, error) {
	row := q.db.QueryRow(ctx, getConsorcio, id)
	var i Consorcio
	err := row.Scan(
		&i.TenantID,
		&i.ID,
		&i.Nombre,
		&i.Cuit,
		&i.Domicilio,
		&i.Tipo,
		&i.Estado,
		&i.Config,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listConsorcios = `-- name: ListConsorcios :many
SELECT tenant_id, id, nombre, cuit, domicilio, tipo, estado, config, created_at, updated_at
FROM consorcios
WHERE tenant_id = app.current_tenant_id()
  AND ($1::TEXT = '' OR nombre ILIKE '%' || $1::TEXT || '%')
  AND ($2::TEXT = '' OR estado = $2::TEXT)
ORDER BY created_at DESC, id DESC
`

type ListConsorciosParams struct {
	Q      string `json:"q"`
	Estado string `json:"estado"`
}

func (q *Queries) ListConsorcios(ctx context.Context, arg ListConsorciosParams) ([]Consorcio, error) {
	rows, err := q.db.Query(ctx, listConsorcios, arg.Q, arg.Estado)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Consorcio{}
	for rows.Next() {
		var i Consorcio
		if err := rows.Scan(
			&i.TenantID,
			&i.ID,
			&i.Nombre,
			&i.Cuit,
			&i.Domicilio,
			&i.Tipo,
			&i.Estado,
			&i.Config,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateConsorcio = `-- name: UpdateConsorcio :one
UPDATE consorcios
SET nombre = COALESCE($1::TEXT, nombre),
    cuit = COALESCE($2::TEXT, cuit),
    domicilio = COALESCE($3::TEXT, domicilio),
    tipo = COALESCE($4::TEXT, tipo),
    estado = COALESCE($5::TEXT, estado),
    updated_at = now()
WHERE tenant_id = app.current_tenant_id()
  AND id = $6::UUID
RETURNING tenant_id, id, nombre, cuit, domicilio, tipo, estado, config, created_at, updated_at
`

type UpdateConsorcioParams struct {
	Nombre    pgtype.Text `json:"nombre"`
	Cuit      pgtype.Text `json:"cuit"`
	Domicilio pgtype.Text `json:"domicilio"`
	Tipo      pgtype.Text `json:"tipo"`
	Estado    pgtype.Text `json:"estado"`
	ID        pgtype.UUID `json:"id"`
}

func (q *Queries) UpdateConsorcio(ctx context.Context, arg UpdateConsorcioParams) (Consorcio, error) {
	row := q.db.QueryRow(ctx, updateConsorcio,
		arg.Nombre,
		arg.Cuit,
		arg.Domicilio,
		arg.Tipo,
		arg.Estado,
		arg.ID,
	)
	var i Consorcio
	err := row.Scan(
		&i.TenantID,
		&i.ID,
		&i.Nombre,
		&i.Cuit,
		&i.Domicilio,
		&i.Tipo,
		&i.Estado,
		&i.Config,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
